{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "vite --port 3000",
    "emulator": "concurrently \"npm run start\" \"webxdc-dev run https://localhost:3000\"",
    "fix": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "@webxdc/vite-plugins": "latest",
    "@webxdc/webxdc-dev": "latest",
    "concurrently": "latest",
    "prettier": "latest",
    "vite": "latest"
  },
  "dependencies": {
    "natlib": "^0.1.15"
  }
}
